# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
!*.tfvars.example
backend.hcl
override.tf
override.tf.json

# Bicep build artifacts
*.json.bicepbuild
main.json

# Azure / secrets
*.pem
*.key
.env
.secrets
azureauth.json

# OS / editor
.DS_Store
.vscode/

# Planning
plan/

# Deploy config (per-operator)
deploy/config.env

# Terraform plan output (binary, regenerated)
tfplan
*.tfplan

# Live inventory holds tenant-specific IDs — tracked as a blank template instead
inventory/decoy-inventory.json

# AzureHound
azurehound.json

# VM keys
decoy_key
decoy_key.pub
